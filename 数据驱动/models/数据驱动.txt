驱动程序：驱动程序是链接内存和其他介质的桥梁

mysql驱动程序: mysql驱动程序是链接内存数据和mysql数据的桥梁

 mysql.createConnection : 创建链接 参数是一个对象 host：'',	user:'root',	password:'密码'

 connection.query :执行命令 参数1 命令 参数2 回调

sql注入:用户通过注入sql语句到最终查询中，导致了整个sql与预期行为不符

防止sql注入：使用extcute()
	const sql = `select * from class where id = ?;`
	const [results] =await connection.execute(sql,[id]);
	console.log(results);
链接池:把所有链接对象保存到一个数组里，避免服务器资源被大量的占用

模型的增删改查
 三层架构
 路由Route(提供对外的API访问接口) ---- 服务层Service(提供业务逻辑支持) --- 数据访问DAO(提供数据库或其他持久的通信通常为ORM)